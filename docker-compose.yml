services:
  bot:
    build: .
    container_name: prostastal_collector
    restart: always
    env_file:
      - .env
      - .env.local
    volumes:
      - ./media:/app/media
      - ./credentials:/app/credentials
      - ./src:/app/src
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
